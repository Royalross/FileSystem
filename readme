Objective: To learn pointers, structs, and command line arguments by implementing a fake file system in C.
Tasks:
Created a program to simulate a file system with directories and files.
Implemented commands such as cd, ls, pwd, cat, cp, mv, rm, mkdir, rmdir, and a custom file command.
Developed additional custom commands to enhance the file system.
Added command line argument support to create files based on directory contents.
Skills Gained: Understanding of file system structures, handling command line arguments, linked data structures, and dynamic memory management in C.
